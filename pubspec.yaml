name: prices_project_rpl5
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  barcode_widget: ^2.0.4
  flutter_barcode_scanner_plus: ^3.0.8
  mobile_scanner: ^3.5.7   
  photo_manager: ^3.0.1+1  
  image_picker: ^1.0.7     
  http: ^1.1.0             
  shared_preferences: ^2.2.2
  vibration: ^3.1.3
  audioplayers: ^5.2.1     
  share_plus: ^7.2.1       
  url_launcher: ^6.2.5
  flashlight_flutter: ^1.0.4     

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/app_icon.png"

flutter:
  uses-material-design: true

  assets:
    - assets/homeScreenbackground.jpg
    - assets/beep_sound.mp3
    - assets/app_icon.png
